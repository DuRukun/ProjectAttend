<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.granger.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".initial">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".studentLogIn" />
        <activity android:name=".lecturerLogIn" />
        <activity android:name=".studentRegister" />
        <activity android:name=".lecturerRegister" />
        <activity android:name=".attendanceTaking" />
        <activity android:name=".lecturerMenu" />
        <activity android:name=".attendanceChecking" />
        <activity android:name=".timetableManagement" />
        <activity android:name=".nameList"></activity>
    </application>

</manifest>